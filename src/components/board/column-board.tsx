"use client";
import { useState,useTransition } from "react";import { ModernPostCard } from "@/components/board/modern-post-card";import { ImagePlus,Loader2,Paperclip,Plus,Trash2,X } from "lucide-react";import { useRouter } from "next/navigation";import { createBoardColumnAction,deleteBoardColumnAction } from "@/app/actions/columns";import { createPostAction } from "@/app/actions/posts";import { uploadBoardFile } from "@/lib/storage/upload-board-file";import type { Board,BoardColumn,Group,Post,UserProfile } from "@/types/database";
type Draft={columnId:string;title:string;content:string;fileUrl:string|null;fileName:string|null;fileType:string|null};const blank=(columnId:string):Draft=>({columnId,title:"",content:"",fileUrl:null,fileName:null,fileType:null});
export function ColumnBoard({board,group,profile,columns,initialPosts}:{board:Board;group:Group;profile:UserProfile;columns:BoardColumn[];initialPosts:Post[]}){const router=useRouter();const[posts,setPosts]=useState(initialPosts);const[draft,setDraft]=useState<Draft|null>(null);const[columnTitle,setColumnTitle]=useState("");const[addingColumn,setAddingColumn]=useState(false);const[uploading,setUploading]=useState(false);const[error,setError]=useState("");const[pending,startTransition]=useTransition();const teacher=profile.role==="teacher";function addColumn(){startTransition(async()=>{const r=await createBoardColumnAction({boardId:board.id,title:columnTitle});if(!r.ok){setError(r.message||"기둥 생성 실패");return}setColumnTitle("");setAddingColumn(false);router.refresh()})}function removeColumn(id:string){if(!confirm("기둥을 삭제할까요? 글은 미분류 상태로 남습니다."))return;startTransition(async()=>{await deleteBoardColumnAction({boardId:board.id,columnId:id});router.refresh()})}async function attach(file?:File){if(!file||!draft)return;setUploading(true);setError("");const r=await uploadBoardFile({file,groupId:group.id,boardId:board.id,userId:profile.id});setUploading(false);if("error" in r){setError(r.error ?? "파일 업로드에 실패했습니다.");return}setDraft({...draft,fileUrl:r.publicUrl,fileName:r.name,fileType:r.type})}function savePost(){if(!draft)return;if(!draft.title.trim()){setError("글 제목은 반드시 입력해 주세요.");return}startTransition(async()=>{const r=await createPostAction({boardId:board.id,columnId:draft.columnId,title:draft.title,content:draft.content,attachmentUrl:draft.fileUrl,attachmentName:draft.fileName,attachmentType:draft.fileType,color:["#FFF3B0","#DDF4E7","#E7E2FF","#FFE2E8"][posts.length%4]});if(!r.ok||!r.post){setError(r.message||"등록 실패");return}setPosts(v=>board.sort_order==="newest"?[r.post!,...v]:[...v,r.post!]);setDraft(null)})}return <div className={`column-board background-${board.background}`}><div className="column-board-head"><div><h2>{board.title}</h2><p>{board.description}</p></div>{teacher?<button className="column-add-main" onClick={()=>setAddingColumn(true)}><Plus/>기둥 만들기</button>:null}</div>{error?<p className="column-error">{error}</p>:null}<div className="column-track">{columns.map(col=><section className="board-column" key={col.id}><header><h3>{col.title}</h3>{teacher?<button onClick={()=>removeColumn(col.id)} aria-label="기둥 삭제"><Trash2/></button>:null}</header><button className="column-post-add" disabled={board.is_locked&&!teacher} onClick={()=>setDraft(blank(col.id))}><Plus/><span>{board.is_locked&&!teacher?"글쓰기가 잠겼어요":"이 기둥에 글 붙이기"}</span></button><div className="column-posts">{posts.filter(p=>p.column_id===col.id).map(post=><ModernPostCard key={post.id} post={post} />)}</div></section>)}{teacher?<button className="empty-column-card" onClick={()=>setAddingColumn(true)}><Plus/><span>새 기둥</span></button>:null}</div>{columns.length===0?<div className="column-empty"><h3>아직 기둥이 없어요</h3><p>선생님이 기둥을 만들면 학생 글을 차곡차곡 모을 수 있어요.</p>{teacher?<button className="primary-button" onClick={()=>setAddingColumn(true)}><Plus/>첫 기둥 만들기</button>:null}</div>:null}{addingColumn?<div className="column-modal-backdrop" onClick={()=>setAddingColumn(false)}><div className="column-dialog small" onClick={e=>e.stopPropagation()}><button className="modal-x" onClick={()=>setAddingColumn(false)}><X/></button><h2>새 기둥 만들기</h2><label>기둥 제목<input value={columnTitle} onChange={e=>setColumnTitle(e.target.value)} autoFocus maxLength={80} placeholder="예: 찬성 의견, 궁금한 점" onKeyDown={e=>{if(e.key==="Enter")addColumn()}}/></label><button className="primary-button" disabled={pending} onClick={addColumn}>{pending?<Loader2 className="animate-spin"/>:<Plus/>}기둥 만들기</button></div></div>:null}{draft?<div className="column-modal-backdrop" onClick={()=>setDraft(null)}><div className="column-dialog" onClick={e=>e.stopPropagation()}><button className="modal-x" onClick={()=>setDraft(null)}><X/></button><span className="dialog-kicker">{columns.find(c=>c.id===draft.columnId)?.title}</span><h2>새 포스트잇 붙이기</h2><label>글 제목 <b>필수</b><input value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})} required maxLength={100} placeholder="글의 제목을 입력하세요"/></label><label>내용<textarea value={draft.content} onChange={e=>setDraft({...draft,content:e.target.value})} rows={7} placeholder="친구들에게 전하고 싶은 생각을 적어 주세요"/></label><label className="file-picker"><input type="file" onChange={e=>void attach(e.target.files?.[0])}/>{uploading?<Loader2 className="animate-spin"/>:<ImagePlus/>}<span>{draft.fileName||"사진, PDF, 한글 등 첨부파일 선택 (20MB 이하)"}</span></label><button className="primary-button post-submit" disabled={pending||uploading} onClick={savePost}>{pending?<Loader2 className="animate-spin"/>:<Paperclip/>}포스트잇 등록</button></div></div>:null}</div>}
